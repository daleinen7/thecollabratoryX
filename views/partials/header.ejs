<!DOCTYPE html>
<html>
  <head>
    <meta charset="utf-8">
    <meta name="viewport" content="width=device-width, initial-scale=1">
    <title><%= title %></title>
    <link rel='stylesheet' href='/../stylesheets/style.css' />
    <script defer src="https://use.fontawesome.com/releases/v5.3.1/js/all.js"></script>
  </head>
  <body>
    <nav>
      <ul>
        <div class="misc-nav">
          <li><a href="/" class="logo"><span class="small-the">the</span> <span class="fl">c</span>ollabratory</a></li>
          
        </div>

        <div class="login">
          <% if (user) { %>
            <li><a href="/logout">Log Out</a></li>
          <% } else { %>
            <li><a href="/auth/google">Login with Google</a></li>
          <% } %>
        </div>
      </ul>
      <% if (user) { %>
        <li><a href="/sets/new" class="add-set">Add New Set</a></li>
      <% } %>
    </nav>